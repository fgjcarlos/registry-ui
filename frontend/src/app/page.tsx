import HomeView from "../components/HomeView";

export const metadata = {
  title: "Registry UI - Home",
  description: "Connect to your Docker registry with ease."
};

export default function Home() {
  return <HomeView />;
}
